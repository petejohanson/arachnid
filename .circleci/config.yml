version: 2
jobs:
  build:
    working_directory: ~/arachnid-build
    docker:
      - image: haskell:8.0
    steps:
      - checkout
      - restore_cache:
          key: stack-cache-{{ checksum "arachnid.cabal" }}
      - save_cache:
          key: stack-cache-{{ checksum "arachnid.cabal" }}
          paths:
            - ~/.stack
      - run:
          name: test
          command: stack test --coverage
