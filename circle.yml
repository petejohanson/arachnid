dependencies:
  cache_directories:
    - "~/.stack"
  override:
    - stack setup
    - stack build --test --no-run-tests --coverage

test:
  override:
    - stack test --coverage
  post:
    - stack install stack-hpc-coveralls && stack exec shc -- --repo-token ${COVERALLS_REPO_TOKEN} arachnid spec doctest
