machine:
  ghc:
    version: 7.8.3

test:
  pre:
    - cabal configure --enable-tests --enable-library-coverage
    - cabal build
  override:
    - cabal test
  post:
    - cabal install hpc-coveralls
    - hpc-coveralls spec