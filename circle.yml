machine:
  ghc:
    version: 7.10.2

test:
  pre:
    - cabal configure --enable-tests --enable-library-coverage
    - cabal build
  override:
    - cabal test
  post:
    - cabal sandbox delete && cabal sandbox init && cabal install hpc-coveralls-1.0.0 && hpc-coveralls spec
