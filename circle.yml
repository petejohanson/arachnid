dependencies:
  cache_directories:
    - "~/.stack"
  override:
    - stack setup
    - stack build

test:
  override:
    - stack test
  post:
    - stack install hpc-coveralls && stack exec hpc-coveralls --repo-token ${COVERALLS_REPO_TOKEN} spec
