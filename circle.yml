machine:
  ghc:
    version: 7.10.2

test:
  pre:
    - cabal configure --enable-tests --enable-library-coverage
    - cabal build
  override:
    - cabal test
  post:
    - cabal install --force-reinstalls hpc-coveralls && .cabal-sandbox/bin/hpc-coveralls --repo-token ${COVERALLS_REPO_TOKEN} spec
