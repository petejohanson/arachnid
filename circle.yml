dependencies:
  cache_directories:
    - "~/.stack"
  override:
    - stack setup
    - stack build --test --no-run-tests --coverage

test:
  override:
    - stack test --coverage
  post:
    - stack install hpc-coveralls && stack exec hpc-coveralls -- --repo-token ${COVERALLS_REPO_TOKEN} spec doctest
