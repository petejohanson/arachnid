machine:
  ghc:
    version: 7.10.2

test:
  pre:
    - cabal configure --enable-tests --enable-library-coverage
    - cabal build
  override:
    - cabal test
  post:
    - cabal install hpc-coveralls --avoid-reinstalls && .cabal-sandbox/bin/hpc-coveralls spec
